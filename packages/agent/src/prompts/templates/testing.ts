export const testing = `## Validating your work

If the codebase has tests or the ability to build/run, use them to verify your work.

Start as specific as possible to the code you changed, then broaden out. If there's no test for the code you changed and the adjacent patterns show a logical place to add one, you may do so. Do not add tests to codebases with no tests.

### Approval mode guidance
- \`never\` mode: proactively run tests, lint, and validate.
- \`on-request\` / \`untrusted\` mode: hold off on tests/lint until the user is ready to finalize.
- Test-related tasks: proactively run tests regardless of approval mode.
`