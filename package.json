{
  "name": "superagent",
  "version": "0.1.0",
  "private": true,
  "description": "Local coding agent powered by @openai/agents",
  "scripts": {
    "dev:server": "npm --prefix server run dev",
    "dev:web": "npm --prefix web run dev",
    "build:packages": "npm --prefix packages/core run build && npm --prefix packages/agent run build && npm --prefix packages/client run build",
    "build:server": "npm --prefix server run build",
    "build:web": "npm --prefix web run build",
    "build": "npm run build:packages && npm run build:server && npm run build:web"
  }
}