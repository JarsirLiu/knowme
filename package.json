{
  "name": "superagent",
  "version": "0.1.0",
  "private": true,
  "description": "Local coding agent powered by @openai/agents",
  "scripts": {
    "dev:server": "pnpm --dir server run dev",
    "dev:web": "pnpm --dir web run dev",
    "build:packages": "pnpm --dir packages/core run build && pnpm --dir packages/agent run build && pnpm --dir packages/client run build",
    "build:server": "pnpm --dir server run build",
    "build:web": "pnpm --dir web run build",
    "test:server": "pnpm --dir server run test",
    "build": "pnpm run build:packages && pnpm run build:server && pnpm run build:web"
  }
}
